# PlatformIO build artifacts
firmware/.pio/
firmware/.vscode/

# OS
.DS_Store

# Claude Code
.claude/

# Node tooling deps (npm install --no-save)
tools/node_modules/
tools/package-lock.json

# Mac daemon venv
daemon/.venv/

# Python bytecode cache
__pycache__/
*.pyc

# Local firmware-image snapshots used by recovery/restore.ps1. The
# .ps1 itself is committed; the multi-MB factory binaries are not.
recovery/*.bin

# One-off serial-screenshot captures used while iterating on the UI.
phaseb_usage.*
